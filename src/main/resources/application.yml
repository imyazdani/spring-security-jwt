spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/spring_jwt?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: update
server:
  port: 8080